# Tasks

- [x] Task 1: Update `src/cli/install.ts` to add detection logic using `fs.existsSync` for each agent in `AGENT_CONFIGS`.
- [x] Task 2: Modify the agent selection array logic to sort and prepend the detected agents with `checked: true`.
- [x] Task 3: Ensure undetected agents are sorted alphabetically and appended after the detected agents.
