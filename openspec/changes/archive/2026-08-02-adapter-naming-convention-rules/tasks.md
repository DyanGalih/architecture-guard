## 1. Update Init Command

- [x] 1.1 Edit `src/commands/init.md` to add "Phase 5 - Naming & Namespacing Conventions"
- [x] 1.2 Update the output generation logic in `init.md` to write the naming rules into the constitution
- [x] 1.3 Add logic to `init.md` to read default naming rules from selected framework presets

## 2. Update Framework Presets

- [x] 2.1 Update at least one existing framework preset (e.g., Laravel or NestJS) to include native naming standard definitions
- [x] 2.2 Add an example preset for a framework with unique conventions (e.g., CodeIgniter 3 using `Class_Model`) to prove the concept

## 3. Verification

- [x] 3.1 Test the `init` command with a generic setup to verify manual prompting works
- [x] 3.2 Test the `init` command with a preset setup to verify auto-detection works
